# ════════════════════════════════════════════════════════════════
#  CloseFast Omni — Vercel Environment Variables
#  Copy to Vercel Dashboard → Settings → Environment Variables
# ════════════════════════════════════════════════════════════════

# ── APP ──────────────────────────────────────────────────────────
NODE_ENV=production
DOMAIN=closefast.tech
JWT_SECRET=your-random-32-char-secret-here-change-this!!

# ── DATABASE (Upstash Redis — FREE at upstash.com) ───────────────
UPSTASH_REDIS_REST_URL=https://YOUR.upstash.io
UPSTASH_REDIS_REST_TOKEN=YOUR_TOKEN

# ── EMAIL (choose one, all have free tier) ───────────────────────
# Resend — recommended (3K/month free) resend.com
RESEND_API_KEY=re_REPLACE

# Brevo — 300/day free brevo.com
# BREVO_API_KEY=xkeysib-REPLACE

# SMTP fallback
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@gmail.com
# SMTP_PASS=app-password-here

# ── FREE AI — TEXT (add key, system uses as fallback) ────────────
# Groq — 14K req/day FREE — console.groq.com
GROQ_API_KEY=gsk_REPLACE

# Google Gemini — 1500 req/day FREE — aistudio.google.com
GEMINI_API_KEY=AIza_REPLACE

# Mistral AI — free tier — console.mistral.ai
MISTRAL_API_KEY=REPLACE

# Together AI — $25 free credits — api.together.ai
TOGETHER_API_KEY=REPLACE

# Cohere — 1K/mo free — dashboard.cohere.com
COHERE_API_KEY=REPLACE

# ── FREE AI — IMAGES ─────────────────────────────────────────────
# Hugging Face — FLUX images free — huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=hf_REPLACE
# Note: Pollinations.ai works with NO KEY (always free fallback)

# Ideogram — free credits — ideogram.ai/api
IDEOGRAM_API_KEY=REPLACE

# ── PREMIUM AI (optional, users can add their own) ───────────────
ANTHROPIC_API_KEY=sk-ant-REPLACE
OPENAI_API_KEY=sk-REPLACE
STABILITY_API_KEY=sk-REPLACE

# ── AI VIDEO (paid, users add their own) ─────────────────────────
RUNWAY_API_KEY=REPLACE
LUMA_API_KEY=REPLACE
KLING_API_KEY=REPLACE
REPLICATE_API_TOKEN=r8_REPLACE
PIKA_API_KEY=REPLACE

# ── BUSINESS APIS ────────────────────────────────────────────────
ETSY_API_KEY=keystring_REPLACE
ETSY_SHOP_ID=VibeprintsProducts
PRINTIFY_API_TOKEN=REPLACE

# ── PAYMENTS ─────────────────────────────────────────────────────
STRIPE_SECRET_KEY=sk_live_REPLACE
STRIPE_PUBLISHABLE_KEY=pk_live_REPLACE
STRIPE_WEBHOOK_SECRET=whsec_REPLACE

# ── BLOCKCHAIN ───────────────────────────────────────────────────
MONAD_RPC_URL=https://rpc.monad.xyz
VLY_TOKEN=0x9459ddd1B70E51280DEf774650EcD04F0e24d234
PLATFORM_WALLET=0x44afc052d7b2f17fd125a8022e5a1964fa35f008

# ── OWNER EMAILS (hardcoded in lib/db.js — no need to change) ────
# volya089@gmail.com → elite + god mode (free)
# slavikbobnar1981@gmail.com → elite + god mode (free)
